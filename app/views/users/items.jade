#items-dashboard
  .clear
  //- each item, i in user.items
  //-   img.item(src=user.items[i].image)

  each item in user.items
    case item.type
      when 'autogrow'
        include ../items/autogrow
      when 'autoseed'
        include ../items/autoseed
